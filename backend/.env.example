# Copie este arquivo para `.env` e preencha os valores.
# Este projeto usa PostgreSQL via Prisma.

# Conexão do banco de dados (formato padrão Prisma)
# Exemplo local: postgresql://usuario:senha@localhost:5432/worki?schema=public
# Exemplo Supabase (SSL obrigatório):
# postgresql://postgres:<PASSWORD>@db.<REF>.supabase.co:5432/postgres?schema=public&sslmode=require
# Se sua rede bloquear 5432, use o pooler do Supabase (porta 6543):
# postgresql://postgres:<PASSWORD>@aws-0-us-east-1.pooler.supabase.com:6543/postgres?schema=public&sslmode=require&pgbouncer=true&connection_limit=1
DATABASE_URL=

# Porta do servidor (opcional)
PORT=8080

# JWT para autenticação
JWT_SECRET=troque_este_segredo

# URL pública do frontend para montar links (reset de senha)
FRONTEND_URL=http://localhost:4200

# Config de e-mail (opcional, usado para reset de senha)
# Você pode usar UMA dessas abordagens:
# 1) SMTP custom:
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# 2) Serviço (ex.: gmail) com user/pass:
MAIL_SERVICE=
MAIL_USER=
MAIL_PASS=
# Compatibilidade com nomes antigos (se preferir):
EMAIL_HOST=
EMAIL_PORT=
EMAIL_SECURE=
EMAIL_USER=
EMAIL_PASS=

# Cloudinary (opcional, usado para avatar/imagens)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
